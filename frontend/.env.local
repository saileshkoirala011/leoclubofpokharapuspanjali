# Backend API — Next.js rewrites proxy all /api/* calls to this URL
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Public site URL (used for OG tags, canonical links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
